# build output
dist
app/renderer
bin/cli.*

# dependencies
node_modules

# logs
npm-debug.log
yarn-error.log

# optional dev config file and plugins directory
.hyper.js
.hyper_plugins

.DS_Store

# ignore generated js and js.map files (files already tracked are not affected, to add a new tracked js file do git add -f
app/**/*.js
app/**/*.js.map

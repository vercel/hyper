machine:
  xcode:
    version: 8.2

dependencies:
  pre:
    - npm install -g yarn
  override:
    - yarn

test:
  override:
    - yarn test

deployment:
  artifacts:
    branch: /.*/
    commands:
      - yarn run dist
      - cp dist/*.zip $CIRCLE_ARTIFACTS
  release:
    tag: /.*/
    commands:
      - yarn run dist

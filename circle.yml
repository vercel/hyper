machine:
  xcode:
    version: 8.2

deployment:
  artifacts:
    branch: /.*/
    commands:
      - npm run dist
      - cp dist/mac/*.dmg $CIRCLE_ARTIFACTS
  release:
    tag: /.*/
    commands:
      - npm run dist
